# 前端 API 基址。默认 "/api"，开发/生产均走同源代理：
#   - 开发：vite dev server 代理 /api -> http://localhost:8000（见 vite.config.ts）
#   - 生产：nginx 代理 /api/ -> http://backend:8000/（见 nginx.conf）
# 仅当前端与后端不同源、且不经过 nginx 代理时才需要改为直连地址：
#   VITE_API_BASE_URL=http://YOUR_SERVER_HOST:8000
VITE_API_BASE_URL=/api

# 高德地图 JavaScript API Key。
# 注意：这里需要前端 JS API 的 key，不是后端 Web 服务 key。
VITE_AMAP_JS_KEY=
